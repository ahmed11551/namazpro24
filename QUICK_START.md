# Быстрый старт

## Установка Node.js

### macOS (через Homebrew)
```bash
brew install node
```

### macOS (через официальный сайт)
1. Скачайте установщик с https://nodejs.org/
2. Выберите LTS версию (рекомендуется 18.x или выше)
3. Установите следуя инструкциям

### Проверка установки
```bash
node --version  # Должно быть v18.x.x или выше
npm --version   # Должно быть 9.x.x или выше
```

## Запуск приложения

### Вариант 1: Использование скрипта (рекомендуется)
```bash
./start.sh
```

### Вариант 2: Ручной запуск
```bash
# 1. Установка зависимостей
npm install

# 2. Создание .env.local (опционально)
cp .env.example .env.local
# Отредактируйте .env.local при необходимости

# 3. Запуск dev сервера
npm run dev
```

### Вариант 3: Через package.json
```bash
npm install && npm run dev
```

## Открытие приложения

После запуска откройте в браузере:
- **Локально**: http://localhost:3000
- **В Telegram**: Настройте Web App URL в настройках бота

## Тестирование без Telegram

Приложение работает и без Telegram, но некоторые функции (например, получение данных пользователя) будут использовать mock данные.

## Структура команд

```bash
npm run dev      # Запуск dev сервера
npm run build    # Сборка для продакшена
npm run start    # Запуск продакшен версии
npm run lint     # Проверка кода
npm run type-check  # Проверка TypeScript
```

## Решение проблем

### Ошибка "command not found: npm"
- Установите Node.js (см. выше)

### Ошибка порта 3000 занят
```bash
# Используйте другой порт
PORT=3001 npm run dev
```

### Ошибки при установке зависимостей
```bash
# Очистите кеш и переустановите
rm -rf node_modules package-lock.json
npm install
```

### Проблемы с TypeScript
```bash
# Проверьте версию TypeScript
npm list typescript
# Если нужно обновить
npm install -D typescript@latest
```

## Следующие шаги

1. ✅ Установите Node.js
2. ✅ Запустите `npm install`
3. ✅ Запустите `npm run dev`
4. ✅ Откройте http://localhost:3000
5. 📱 Настройте Telegram бота (опционально)

## Полезные ссылки

- [Node.js](https://nodejs.org/)
- [Next.js документация](https://nextjs.org/docs)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)

