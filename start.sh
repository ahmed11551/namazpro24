#!/bin/bash

# Скрипт для запуска приложения

echo "🚀 Запуск NamazPro24..."

# Проверка Node.js
if ! command -v node &> /dev/null; then
    echo "❌ Node.js не установлен!"
    echo ""
    echo "Установите Node.js версии 18 или выше:"
    echo "  macOS: brew install node"
    echo "  или скачайте с https://nodejs.org/"
    exit 1
fi

# Проверка версии Node.js
NODE_VERSION=$(node -v | cut -d'v' -f2 | cut -d'.' -f1)
if [ "$NODE_VERSION" -lt 18 ]; then
    echo "❌ Требуется Node.js версии 18 или выше. Текущая версия: $(node -v)"
    exit 1
fi

echo "✅ Node.js $(node -v) установлен"

# Проверка зависимостей
if [ ! -d "node_modules" ]; then
    echo "📦 Установка зависимостей..."
    npm install
fi

# Проверка .env файла
if [ ! -f ".env.local" ]; then
    echo "⚠️  Файл .env.local не найден"
    echo "Создайте .env.local с необходимыми переменными окружения"
    echo "См. README.md для подробностей"
fi

# Запуск dev сервера
echo "🌐 Запуск dev сервера..."
echo "Откройте http://localhost:3000 в браузере"
echo ""
npm run dev

